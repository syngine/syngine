using Microsoft.Xna.Framework;

namespace Syngine
{
    public static class GameServicesContainerExtensions
    {
        //public static TService GetService<TService>(this GameServiceContainer container)
        //{
        //    return (TService)container.GetService(typeof(TService));
        //}
        //public static void AddService<TService>(this GameServiceContainer container, TService service)
        //{
        //    container.AddService(service);
        //}
    }
}